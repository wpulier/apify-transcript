{
  "title": "Large Video to Transcript",
  "description": "Submit a video or audio file. We create the MP3, timestamped transcript exports, quality report, and ZIP bundle.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "media": {
      "title": "Submit video or audio",
      "description": "Upload a file or paste a direct media URL. For files over 500 MB, a direct URL is faster than browser upload.",
      "type": "array",
      "editor": "fileupload",
      "minItems": 1,
      "maxItems": 10,
      "items": {
        "type": "string"
      },
      "prefill": [
        "https://raw.githubusercontent.com/wpulier/apify-transcript/main/samples/large-video-to-transcript-sample.mp3"
      ]
    }
  },
  "required": ["media"]
}
