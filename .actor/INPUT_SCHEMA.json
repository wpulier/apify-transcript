{
  "title": "Large Video to Transcript",
  "description": "Upload a video or audio file, click the green Start / Build & start button, and download the MP3 plus transcript bundle when it finishes.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "media": {
      "title": "Upload video or audio",
      "description": "Click Upload new files to choose an MP4, MOV, WebM, MP3, M4A, or WAV file. You can also paste a direct downloadable media URL in the same field. Then click the green Start / Build & start button at the bottom left.",
      "type": "array",
      "editor": "fileupload",
      "minItems": 1,
      "maxItems": 10,
      "items": {
        "type": "string"
      }
    }
  },
  "required": ["media"]
}
