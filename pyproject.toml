[project]
name = "apify-transcript"
version = "0.1.0"
description = "Large video and audio transcription Apify Actor"
requires-python = ">=3.11"
dependencies = [
  "apify>=2.0.0",
  "httpx>=0.27.0",
  "openai>=1.84.0"
]

[tool.setuptools.packages.find]
where = ["src"]

